# для начала надо посмотреть есть ли разделы созданные с lvm , для этого используется команда sudo pvs
# далее PFree показывает что этот диск не привязан к какой либо группе VG  
# далее его можно удалить  следующей командой sudo pvremove /dev/vdb1
